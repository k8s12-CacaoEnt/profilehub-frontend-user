import React from 'react';

function LoginError() {
    return (
        <div className="LoginError">
            <h2>Login Error</h2>
            <button>Retry</button>
        </div>
    );
}

export default LoginError;
